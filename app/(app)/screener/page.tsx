import DashboardClient from "@/features/dashboard/components/DashboardClient";

export const metadata = { title: "Screener — OrcaTrading" };

export default function ScreenerPage() {
  return <DashboardClient />;
}
